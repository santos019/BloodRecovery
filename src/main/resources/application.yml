server:
  port: 8003

spring:
  datasource:
#    url: jdbc:mysql://ec2-3-144-213-215.us-east-2.compute.amazonaws.com:3306/cardDB
    url: jdbc:mysql://127.0.0.1:3306/mainDB
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: prod

#spring:
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/blood_card
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  profiles:
#    active: prod

  jpa:
    hibernate:
      ddl-auto: update #나중에 none으로 바꿔
    properties:
      hibernate:
        #         show_sql: true
        # show_sql : 옵션은 System.out 에 하이버네이트 실행 SQL을 남긴다.
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
  #  org.hibernate.SQL : 옵션은 logger를 통해 하이버네이트 실행 SQL을 남긴다

